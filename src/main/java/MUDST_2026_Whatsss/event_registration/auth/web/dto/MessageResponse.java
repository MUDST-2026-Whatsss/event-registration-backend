package MUDST_2026_Whatsss.event_registration.auth.web.dto;

/** A neutral acknowledgement for endpoints that must not reveal whether an account exists. */
public record MessageResponse(String message) {
}
