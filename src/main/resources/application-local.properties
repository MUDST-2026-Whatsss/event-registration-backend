# Local development overrides. Never active in a deployed environment.

# Local development is plain HTTP, where a Secure cookie would never be sent.
# Any deployed environment must leave the default (true) in place.
event.auth.cookie-secure=${COOKIE_SECURE:false}
event.auth.registration-auto-verify=true

logging.level.MUDST_2026_Whatsss.event_registration=DEBUG
